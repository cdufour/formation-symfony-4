{% extends "demo.base.html.twig" %}

{% block pageTitle %}demo 27{% endblock %}

{% block body %}
    <h3>Ajout d'un étudiant</h3>

    {# https://symfony.com/doc/4.4/form/form_customization.html #}

    {# form(form) #}

    {{ form_start(form) }} {# <form> #}

    <div class="form_row">
        {{ form_row(form.name) }}
    </div>

        <div class="form_row">
        {{ form_row(form.email) }}
    </div>

    {{ form_end(form) }} {# </form> #}

{% endblock %}