{% extends "demo.base.html.twig" %}

{% block pageTitle %}demo 33{% endblock %}

{% block body %}

    <h3>Test Event</h3>
    <p>{{ "15" | price("EUR") }}</p>
    <p>{{ tva(100) | price("EUR") }}</p>
    <p>{{ square(6) | price("USD") }}</p>

    {% if is_granted('ROLE_ADMIN') %}
        <h2>Etudiants</h2>
        {% for s in students %}
        <div>{{s.name}}</div>
        {% endfor %}
    {% endif %}


{% endblock %}